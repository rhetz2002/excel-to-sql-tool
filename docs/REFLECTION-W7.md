1. The main thing that surprised me was when I was testing how it would behave if it tried to output to an invalid output file. I created an empty jpg file among others, and it all exported without fail. It wasn't until I took an actual image file where it actually failed. this makes sense in hindsight because if I remember correctly file extensions are mostly there to denote to the computer how to open or use the file rather than anything else. Therefor if a program that doesn't know any better sees an empty file, all it sees (in this case) is an output destination. I'm assuming the reason it finally failed when I used a proper image file was do to the way the present data was structured.

2. If I had one more week of testing, I would test the re-prompt logic more thoroughly. It has taken the place of the testing logic in my mind as the main sticking point for me. I'm verry happy with the testing logic but now I see that the re-prompt logic fails after a second prompt. I already plan on spending most of next week to fix up the re-prompt as well as possibly fixing up an issue with mismatched db schemas where the name matches. But mainly the re-prompt is one of the most pressing issues in my opinion. If I could test it a bit more to get to the root of the issue. But as usual I have ideas. 

3. I feel my opinion on good quality code remains largely unchanged, barring two additions. For one I feel I have a higher emphasis on readability because as the code base has grown its become more and more unwieldy. especially validation.py as it has 340 lines which has made it hard to work with. Iv been throwing around the idea of splitting it into two separate files to help with this. number two is trying to abide by good coding practices as it is bugging me how much I use if else and try except and the such as I know that's not good coding practices.  